<div class="border-bottom border-secondary">
    <div class="container text-secondary-emphasis">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <!-- Logo -->
                <a class="navbar-brand" href="{{ path('app_accueil') }}">
                    <img src="{{ asset('uploads/logo/logo.png') }}" alt="Logo WatchList" height="70">
                </a>
                <a class="navbar-brand fw-bold" href="{{ path('app_accueil') }}" style="color:rgb(113, 73, 189)">VELVET RECORD</a>

                <!-- Bouton toggle for mobile -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Bar de navigation et bar de recherche -->
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav d-flex justify-content-center flex-grow-1 mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_accueil') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_add') }}">Formulaire d'ajout</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Contact</a>
                        </li>
                    </ul>

                    <!-- Bar de recherche -->
                    <form class="d-flex" role="search" method="GET" action="{{path('app_search')}}">
                        <div class="position-relative">
                            <input type="text" name="query"
                                   class="form-control rounded-pill pe-5"
                                   placeholder="Rechercher un album..."
                                   aria-label="Rechercher">
                            <button class="btn position-absolute top-50 end-0 translate-middle-y" type="submit">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                                     class="bi bi-search" viewBox="0 0 16 16">
                                    <path
                                        d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
                                </svg>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </nav>
    </div>
</div>